import React from 'react';
import MovieMemo from './movieMemo';

export default DisplayCaptureSurfaceType = (props) => {
  return (
    <div>
      <div>
        <p> Basic Movie Compoenent with React.memo() </p>
        <MovieMemo />
      </div>
    </div>
  );
};
